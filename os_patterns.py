"""
A collection of patterns of the internal os package. This includes
  - Safely create a potentially already existing, and potentially nested, folder.
  - Iterate over all files in a subfolder and read them
"""

import os


def create_directory_if_not_exists(path):
    pass


def iterate_subfolders(root_path):
    pass
